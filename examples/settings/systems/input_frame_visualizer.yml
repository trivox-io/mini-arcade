# Example-specific settings profile:
# Settings.for_example("systems/input_frame_visualizer")
scene:
  initial_scene: input_frame_visualizer
  scene_registry:
    discover_packages:
      - examples.catalog.systems.input_frame_visualizer
      - mini_arcade_core.scenes

engine_config:
  fps: 60
  virtual_resolution: [800, 600]
  enable_profiler: false
  postfx:
    enabled: false
    active: []

backend:
  provider: pygame
  window:
    width: 960
    height: 540
    title: systems/input_frame_visualizer
    resizable: true
  renderer:
    background_color: [16, 18, 28]
  audio:
    enable: false

cli_examples:
  - "--backend pygame"
  - "--backend native"
  - "--virtual-width 960 --virtual-height 540"
