# Example-specific settings profile:
# Settings.for_example("commands/effect_and_debug_hotkeys")
scene:
  initial_scene: hotkeys_demo
  scene_registry:
    discover_packages:
      - examples.catalog.commands.effect_and_debug_hotkeys
      - mini_arcade_core.scenes

engine_config:
  fps: 60
  virtual_resolution: [800, 600]
  enable_profiler: false
  postfx:
    enabled: false
    active: []

backend:
  provider: pygame
  window:
    width: 960
    height: 540
    title: commands/effect_and_debug_hotkeys
    resizable: true
  renderer:
    background_color: [16, 18, 28]
  audio:
    enable: false

cli_examples:
  - "--backend pygame"
  - "--backend native"
  - "--virtual-width 960 --virtual-height 540"
