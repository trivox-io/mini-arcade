# mini-arcade-pygame-backend

Pygame backend for Mini Arcade.

This backend implements the Mini Arcade backend interface using pygame:

- window creation
- event polling → core input events
- primitive drawing and/or sprite blitting (as implemented)
- frame presentation

## When to use it

Use **pygame** when you want:

- simplest setup
- fast iteration and debugging
- a reliable reference backend

## Install

```bash
pip install mini-arcade-pygame-backend
```

## Docs

Architecture and concepts live in the monorepo docs (`docs/`).
