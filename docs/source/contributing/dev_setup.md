# Dev setup

## One-command install (Windows)

```powershell
.\scripts\dev_install.ps1
```

## Notes

- Prefer editable installs for packages under `packages/*`.
- Keep tooling consistent across packages (formatting, linting, typing, tests).
